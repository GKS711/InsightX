# InsightX Python Dependencies (v6.0.0-alpha)
# Install: pip install -r requirements.txt
# 對齊 pyproject.toml；此檔供 Dockerfile 使用

# Core web stack
fastapi>=0.109.0
uvicorn>=0.27.0
python-dotenv>=1.0.0
beautifulsoup4>=4.12.3
jinja2>=3.1.3
requests>=2.31.0
certifi>=2024.0

# LLM + scrapers
google-genai>=1.59.0
youtube-comment-downloader>=0.1.76

# v6: sync persistence + Turso/libsql
# Codex deploy-review fix (MINOR): pin libsql pair to exact versions because
# they're 0.x-versioned and a minor bump may break SQLAlchemy dialect compat.
# Other deps stay as >= since they have stable APIs at this major version.
sqlalchemy>=2.0
alembic>=1.13
sqlalchemy-libsql==0.2.0   # Turso/libsql dialect (sync; async not supported as of 2026-05)
libsql-experimental==0.0.55  # underlying libsql Python bindings (linux+macOS)

# Background workers (file present but not actively used in v6.0.0-alpha)
redis>=5.0
rq>=1.15

# Reports
reportlab>=4.0
python-docx>=1.1

# Notes:
# - aiosqlite / asyncpg removed in v6 (whole stack switched to sync)
# - greenlet still needed transitively by SQLAlchemy 2.0 for some sync-engine paths
