# InsightX - AI 顧客評論分析平台

> **版本**：UI v4.0.0 / API v3.0.0（2026-04-22 起穩定）
> **蝦皮模組已正式放棄**（→ `docs/archive/shopee_evaluation_2026-04-21.md`）
> **詳細交接**：`HANDOFF.md`

## 專案概述

InsightX 是一個 AI 驅動的評論 / 留言分析平台，目前支援兩個模式：

- **店家評論模式**（Google Maps）：輸入店家網址 → Serper 爬取評論 → Gemini 生成餐廳／零售向分析
- **YouTube 頻道模式**（v2.0.0 新增）：輸入單支影片網址 → YouTube Data API 抓留言 → Gemini 生成創作者／頻道成長向分析

兩個模式共享同一組 9 個下游 AI 功能（情緒分析、SWOT、回覆、行銷、根源分析、週計畫、培訓劇本、內部信、對話顧問），每個功能依 `platform` 切換 prompt persona。

## 技術架構

| 元件 | 技術 |
|------|------|
| 後端 | FastAPI (Python >=3.10) |
| 前端（**現行主入口** `/`） | `src/static/v2/index.html`（v2-design，React 18 UMD + @babel/standalone 單一 Babel block；ES module core/hooks 經 `bootstrap.js` 橋接到 `window.IX`。實際行數見 `node validate_jsx.cjs` 輸出） |
| 前端（legacy `/legacy`） | `src/static/index.html`（v3 純 HTML + Tailwind CDN + marked.js，蝦皮已移除；保留用於回退比對） |
| 店家爬蟲 | Serper API（`/maps` + `/reviews`），零瀏覽器 |
| YouTube 爬蟲 | 主：YouTube Data API v3；備：youtube-comment-downloader（fallback，無 API key），零瀏覽器 |
| AI | Google Gemini API (`gemma-4-31b-it`) |

## 本地啟動

```bash
# 後端（從 InsightX/ 目錄）
cd /Users/gankaisheng/VScode/Claude實作/InsightX
source ../venv/bin/activate   # venv 在上層共用目錄
uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload

# 前端（開發模式）
npm run dev

# 前端（打包後由 FastAPI 提供 dist/ 靜態檔）
npm run build
```

瀏覽器測試一律使用 Chrome。

## 環境變數（.env）

| 變數 | 必要 | 說明 |
|------|------|------|
| `GEMINI_API_KEY` | 是 | Google Gemini API Key |
| `SERPER_API_KEY` | 店家模式必填 | Serper API，店家評論爬蟲使用 |
| `YOUTUBE_API_KEY` | 建議 | YouTube Data API v3 Key（主要路徑；沒設定則走 youtube-comment-downloader 備用） |
| `YOUTUBE_FALLBACK_MODE` | 否 | `auto`（預設）／`force-ytdlp`（強制備用）／`off`（關閉 fallback） |
| `ENVIRONMENT` | 否 | `development` 或 `production` |

## 專案結構

```
InsightX/
├── src/
│   ├── main.py                  # FastAPI 入口，掛載路由 + 靜態檔
│   ├── api/routes.py            # 所有 API endpoints
│   ├── services/
│   │   ├── scraper_service.py   # 統一入口；依 URL 自動 dispatch 到 Google Maps / YouTube
│   │   ├── youtube_scraper.py   # (v2.0.0) YouTube Data API v3 留言爬蟲
│   │   └── llm_service.py       # Gemini AI 呼叫（所有方法支援 platform="google"|"youtube"）
│   ├── config/
│   │   ├── prompts.py           # 所有 AI prompt 模板
│   │   └── mock_responses.py    # Gemini 失敗時的 fallback 回應
│   ├── services/canonicalizer.py # (v4) yt_role canonicalize + metadata 包裝
│   └── static/                  # 前端
│       ├── v2/                  # v4 UI 主入口（現行 `/`）
│       │   ├── index.html       # ~3546 行（Babel block 內 3391 行 JSX）
│       │   ├── bootstrap.js     # ES module → window.IX 橋接
│       │   ├── core/            # adapters / ids / async / api
│       │   └── hooks/           # useAppReducer / useAnalyzeStream / useLocalStorage
│       ├── index.html           # 舊 v3 UI（現在掛 `/legacy`）
│       ├── App.tsx              # 獨立小遊戲（不是主分析介面）
│       └── insightx_game.html   # 互動小遊戲
├── docs/
│   ├── v3-ui-migration-plan.md  # v4 UI 遷移計畫書
│   ├── v4-smoke-test.md         # v4 本機 E2E smoke checklist
│   ├── v4-api-contract.md       # v4 API 契約
│   ├── v4-sse-events.md         # v4 SSE event 規格
│   └── v4-view-model.md         # v4 前端 view-model 規格
├── validate_jsx.cjs             # @babel/parser JSX 驗證腳本
├── pyproject.toml               # Python 依賴（uv）
├── package.json                 # 前端依賴（npm）
├── Dockerfile / compose.yaml    # Docker 部署
└── tests/                       # 測試腳本
```

## API Endpoints

所有 AI 端點（`/analyze`、`/swot`、`/reply`、`/analyze-issue`、`/marketing`、`/weekly-plan`、`/training-script`、`/internal-email`、`/chat`）都接受可選欄位 `platform: "google" | "youtube"`，預設 `"google"`。`/analyze` 額外會從 URL 自動偵測平台（`is_youtube_url()`），並以爬蟲回傳的 `platform` 為準覆蓋前端送來的值。

| 方法 | 路徑 | 說明 |
|------|------|------|
| POST | `/api/analyze` | 主流程：URL 偵測平台 → 爬蟲 → AI 分析 |
| GET | `/api/analyze-stream?url=...` | SSE 即時進度串流（平台感知的 log 文案） |
| POST | `/api/swot` | 從 good/bad 數據生成 SWOT（支援 platform） |
| POST | `/api/reply` | 生成回覆（餐廳抱怨 / YouTube 觀眾留言） |
| POST | `/api/analyze-issue` | 根源問題分析 |
| POST | `/api/marketing` | 行銷文案生成（門市活動 / 新影片宣傳） |
| POST | `/api/weekly-plan` | 週行動計畫（營運週 / 頻道創作週） |
| POST | `/api/training-script` | 培訓劇本（員工 / 剪輯師、企劃） |
| POST | `/api/internal-email` | 內部信（門市週報 / 頻道團隊週報） |
| POST | `/api/chat` | AI 顧問（營運顧問 / 頻道成長顧問） |
| POST | `/api/debug-scrape` | 除錯用爬蟲 |

## Google Maps 評論爬蟲 — 核心技術筆記（v13.1）

### 架構：純 Serper API，零瀏覽器

```
scraper_service.py v13.1 流程：
  1. 店名解析：HTTP GET + Mobile UA → Google 302 redirect → 完整 Maps URL → 從 path 提取店名
  2. Serper /maps：用店名搜尋 → 取得 cid、評分、地址等基本資訊
  3. Serper /reviews：用 cid + nextPageToken 分頁 → 取得所有有文字的評論
```

### Serper API Endpoints

| Endpoint | 用途 | 回傳 |
|----------|------|------|
| `POST google.serper.dev/maps` | 搜尋店家 | places[0] 含 title, rating, ratingCount, cid, address |
| `POST google.serper.dev/reviews` | 取得評論 | reviews[] 含 snippet, rating, author, date + nextPageToken |

### 分頁機制

`/reviews` 每頁最多 20 則。回傳包含 `nextPageToken`，將其放入下一次請求的 payload 即可翻頁。迴圈直到回傳的 reviews 少於 20 則或無 nextPageToken。

### 短網址解析（maps.app.goo.gl）

Desktop UA → 200 + JS redirect（無法跟隨）
**Mobile UA → 302 redirect → 完整 Google Maps URL**（關鍵發現）

### 驗證數據

- 全家新竹富美二店（31 則評論）：20 則有文字 ✅
- 全家新竹濱海店（151 則評論）：86 則有文字，8 頁分頁 ✅

### 歷史筆記（已棄用的方法）

以下方法已嘗試並確認無效，保留供參考：
- **Google Maps 內部 API**（listentitiesreviews）：需要登入 cookies，匿名/headless 無法使用
- **UC headless Chrome**：Chrome 147 + `--headless=new` 被 Google 偵測，頁面不含評論資料
- **Playwright headless**：同樣被偵測
- **Direct HTTP + requests**：短網址 JS redirect 無法跟隨，API 需要 cookies

## YouTube 留言爬蟲（v2.0.0 雙路徑設計）

`src/services/youtube_scraper.py`：主／備雙層，零瀏覽器。

```
YouTubeScraper.scrape_video(url) 路由規則：
  1. 解析 video_id
  2. 決定走哪條路徑：
     - YOUTUBE_FALLBACK_MODE=force-ytdlp        → 直接走備用 library
     - 沒有 YOUTUBE_API_KEY 且 mode != off      → 直接走備用 library
     - 預設：先走官方 API，失敗時若 mode != off → 自動 fallback 到備用 library
```

> 備註：env var 值仍寫 `force-ytdlp` 是為了向下相容；實際備用 library 已換成 `youtube-comment-downloader`（詳見路徑 2）。

### 路徑 1：YouTube Data API v3（主）
- `videos.list` + `commentThreads.list`，`order="relevance"`，每頁 100 則 top-level
- 免費配額 10,000 units/day（單支影片通常 3–10 units）
- 需要 `YOUTUBE_API_KEY`

### 路徑 2：youtube-comment-downloader（備用）
- `YoutubeCommentDownloader().get_comments(video_id, sort_by=SORT_BY_POPULAR)` — generator
- 無需 API key、無配額上限；直接打 YouTube 內部 AJAX endpoint
- 影片 metadata（標題、頻道名）由 oEmbed（`/oembed?url=...&format=json`）補齊
- 觸發時機：沒 API key、官方 API 回 403（quota 爆 / key 失效）、`force-ytdlp` 強制模式
- `video_data["source"]` 會標記 `"yt-dlp"`（字串保留以利前端相容）

**為什麼從 yt-dlp 換成 youtube-comment-downloader**：
- yt-dlp 2026.03.17 版的 `max_comments` 在新 YouTube JS runtime 上不再嚴格遵守，會在 spam 多的老影片上無限翻頁
- youtube-comment-downloader 是 generator，`itertools.islice(gen, N)` 精確取 N 則
- 單一職責、輕量、穩定

**為什麼要 fallback**：
- 官方 API 配額爆了時產品還能跑
- 使用者剛申請新 Google Cloud 專案但還沒啟用 API 時仍可 demo

**回傳 shape 兩路徑一致**（store_name=影片標題、rating=like_count、rating_count=view_count），讓下游 9 個 LLM 功能無縫共用。備用路徑 like_count / view_count 會是 0（oEmbed 不提供），但不影響 AI 分析。

## LLM Service

使用 `google-genai` SDK，模型 `gemma-4-31b-it`。結構化輸出使用 `response_mime_type="application/json"`。

9 個方法皆支援 `platform: str = "google"` 參數，內部 if/else 分支切換 prompt：
- `google` 版本 = 餐廳／零售向 persona
- `youtube` 版本 = YouTuber／頻道成長向 persona（剪輯師、觀眾留言、影片企劃等詞彙）

功能：`analyze_content`, `generate_swot`, `generate_reply`, `generate_marketing`, `generate_root_cause_analysis`, `generate_weekly_plan`, `generate_training_script`, `generate_internal_email`, `chat`

## 前端（v4 UI：`src/static/v2/index.html`）

**架構**：單檔 ~3546 行（其中 Babel block 內 3391 行 JSX）React 18（UMD）+ `@babel/standalone` 編譯的 `<script type="text/babel">`。旁邊的 `core/` + `hooks/` 是 ES module，由 `bootstrap.js` 匯入後 freeze 成 `window.IX`，Babel block 用 `useIX()` 取出 `ix.state / ix.dispatch / ix.lib.Adapters`。

**主要元件**：
- `LandingPage`：首頁 + Google/YouTube tab 切換 + 角色選擇 + `useAnalyzeStream` 連 `/api/v4/analyze-stream`
- `ShopDashboard` / `YouTubeDashboard`：接 `buildShopViewModel` / `buildYouTubeViewModel`，`unwrapAnalyze` 解 SSE result 的 `{data: {...}}` 包裝
- `ShopHero` / `ShopThemesSection` / `ShopSwotSection`（SWOT 自動 fetchSwot）
- `ShopRepliesSection`（per-topic 本地 cache）
- `ShopWeekPlanSection` / `ShopMarketingSection`（mock 視覺 baseline + AI Markdown 卡）
- `AiToolsPanel`（培訓劇本 / 內部週報 / 根源分析 三合一）
- `ChatDock`（右下角 chat 面板，DashNav 問 AI 按鈕觸發）

**狀態管理**：
- `useAppReducer`：slice reducer（features.{swot,reply,marketing,weeklyPlan,trainingScript,internalEmail,rootCause,chat} + analyze + stream + alerts + meta + prefs）
- `requestId` stale discard：每個 `FETCH_X_START` 帶新 ID，`SUCCEEDED/FAILED` 只在 ID 相符時寫入
- localStorage persist（白名單：prefs/analyze/ui，不含 stream/chat/alerts/meta/features）

**驗證**：`node validate_jsx.cjs` 跑 @babel/parser 驗整份 JSX 可解析（輸出 `OK · lines: <當前行數>`）。

**注意**：`App.tsx` 是獨立的決策模擬小遊戲，**不是**主分析介面。

## 蝦皮模式放棄紀錄

評估 8 條路線後因蝦皮 TW WAF 加密無解，正式放棄。完整紀錄：[docs/archive/shopee_evaluation_2026-04-21.md](docs/archive/shopee_evaluation_2026-04-21.md)（永久 tracked，CLAUDE.md 在 .gitignore）

## 下次接手要做的（按優先序）

1. **跑 v4 UI smoke test**（`docs/v4-smoke-test.md`）確認 9 endpoint 全活
2. **YouTube fallback 路徑驗證**：`YOUTUBE_FALLBACK_MODE=force-ytdlp` 模式跑一輪確認沒壞
3. **長期規劃 v5**：考慮加 Threads / IG 留言來源（同樣 9 個下游 AI 功能可重用）

## 注意事項

- venv 路徑：`/Users/gankaisheng/VScode/Claude實作/InsightX/.venv/`
- `.env` 必須包含 `SERPER_API_KEY`（店家模式）與 `YOUTUBE_API_KEY`（YouTube 模式）
- `python-dotenv` 載入 `.env`（兩個 scraper 都已 `load_dotenv()`）
- 問題追蹤：`ISSUES_RESOLVED.md`
- 爬蟲研究筆記：`RESEARCH_google_maps_scraping.md`
- 交接文件：`HANDOFF.md`
